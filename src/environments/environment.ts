export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000/api',
  baseUrl: 'http://localhost:3000',
  // Empty in dev — logout falls back to the local /auth/login page
  hubUrl: ''
};
